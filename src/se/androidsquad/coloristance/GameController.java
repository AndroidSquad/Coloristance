package se.androidsquad.coloristance;

public class GameController {
	
	/*Koordinerar mellan View och Model.Bör innehålla metoderna:
	 * 
	 * doorClick()
	 * 	- changeRoom()
	 * 	- changeMapLocation()
	 * 	- animateDoor()
	 * 
	 * drawMap(Grid) tar rutnät från modell
	 * 	- here(getPosition) tar emot position från PositionModel
	 * 	- Neighbours(Door, Connect) tar emot dörrar och de olika dörrarnas relationer till varandra
	 * 	- Rects(color) Hämtar info från modellen om vilka färger rektanglarna ska ha i kartan
	 * 
	 * drawRoom()
	 * 	- drawRect(color) tar info om vilken färg rummets ruta ska målas i
	 * 	- drawDoor(position) tar info om vart dörrarna ska placeras i rummet
	 */

}
